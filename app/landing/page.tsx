import React from 'react'

export default function page() {
  return (
    <div>puedes editar todo lo de dentro del return como un html normal
        en vez de usar "class" para clases se usa "className"
        Dentro del return solo puede haber una etiqueta padre, es decir,
        todo lo que este aqui debe estar dentro de una sola etiqueta
    </div>
  )
}
